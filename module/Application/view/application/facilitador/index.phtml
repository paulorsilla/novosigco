<?php
use Doctrine\ORM\Query\AST\Functions\SubstringFunction;
?>
<div id="tituloPagina">Cadastro de facilitadores<br/><br/>Facilitadores cadastrados</div>


<table class="hover" id="consulta">
<thead>
<tr>
<th style="text-align: left;">Nome</th>
<th style="text-align: left;">Email</th>
<th style="text-align: left;">Telefone celular</th>
<th style="text-align: left;">Currículo</th>
<th style="text-align: left;">Instituição</th>
<th width="90" class="center">Opções</th>
</tr>
</thead>
<tbody>
<?php foreach($facilitadores as $facilitador):?>
		<tr>
			<td><?php echo $this->escapeHtml($facilitador->getNome());?></td>
			<td><?php echo $this->escapeHtml($facilitador->getEmail());?></td>
			<td><?php echo $this->escapeHtml($facilitador->getCelular());?></td>
			<td><?php echo $this->escapeHtml($facilitador->getCurriculo());?></td>
			<td><?php echo $this->escapeHtml($facilitador->getInstituicao()->getRazao());?></td>
			<td style="text-align:center">
				<a href="/application/facilitador/save/id/<?php echo $facilitador->getId();?>"><span title="Editar" class="ui-icon ui-icon-pencil"/></a>
				<a href="/application/facilitador/delete/id/<?php echo $facilitador->getId();?>" id="excluir"><span title="Excluir" class="ui-icon ui-icon-trash"/></a>
			</td> 
		</tr>
	<?php endforeach;?>
</table>
<div id="barraBotoes">
	<button id="novo">Novo...</button>
</div>
<div id="dialog-form" title="Confirmar exclusão">
	<p id="texto"/>
</div>

<script>
	$("#novo").button({
		icons: {
			primary: "ui-icon-plus",
		},
	text: true,
	});
	$("#novo").click(function(e) {
		e.preventDefault();
		window.location ="/application/facilitador/save";
	});
	
    $("input[type=submit], a" ).button();

    $("a#excluir").on("click", function(e) {
        e.preventDefault();
        var link = $(this).attr('href');
        var nome = $(this).closest('tr').find('#nome').text();
        $("#texto").html("Deseja realmente excluir '"+nome+"'?");
        $("#dialog-form").data("link", link);
        $("#dialog-form").dialog("open");
    });

     $( "#dialog-form" ).dialog({
		dialogClass: 'no-close',
        autoOpen: false,
        resizable: false,
        draggable: false,
        height: 200,
        width: 300,
        modal: true,
        buttons: {
        		"Sim": function() {
        		var link = $(this).data("link");
            	window.location = link;
         	  },
     			"Não": function() {
         		$(this).dialog("close");
         	}
	}
    });
   
</script>
